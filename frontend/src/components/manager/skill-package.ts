// Fonctions pures de parsing SKILL.md — ré-exportées depuis le module core
// (sans dépendance i18n, testable hors bundler).
export {
  findSkillMarkdownPath,
  normalizeSkillTags,
  parseSkillMarkdown,
  type ParsedSkillMarkdown,
} from "./skill-package-core"
